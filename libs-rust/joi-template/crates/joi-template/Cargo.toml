[package]
name = "joi-template"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core library for the joi-template project"

[dependencies]
joi-base = { path = "../../../joi-base" }
serde_json = "1.0"

[lib]
path = "src/lib.rs"
