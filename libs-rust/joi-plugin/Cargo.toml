[package]
name = "joi-plugin"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Typed in-process plugin and extension registration for JOI applications"
publish = false

[dependencies]
joi-base = { path = "../joi-base" }
joi-error = { path = "../joi-error" }

